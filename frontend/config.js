// ScraperAgent frontend configuration.
// Point API_BASE_URL at your deployed backend.
const CONFIG = {
  API_BASE_URL: "https://scraperagent-api.onrender.com",
  // Optional shared key. If the backend runs with API_KEY set, clients must
  // send it via the X-API-Key header. Leave empty when not used.
  API_KEY: "",
  MAX_CANDIDATES: 10,
};
